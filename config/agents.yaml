# 에이전트 설정 파일
# 새로운 에이전트 추가 시 이 파일만 수정하면 됨

agents:
  firefighting:
    display_name: "선박 소방 규정"
    description: "선박 소방 시스템 및 SOLAS 규정 전문가"
    module_path: "agents.firefighting_agent.agent"
    bedrock_agent_id: "WT3ZJ25XCL"
    bedrock_alias_id: "3RWZZLJDY1"
    knowledge_base_id: "ZGBA1R5CS0"
    lambda_function_name: "firefighting-lambda"
    enabled: true
    ui_config:
      icon: "🚢"
      color: "#FF6B6B"
      topics:
        - "고정식 소화 시스템"
        - "휴대용 소화기"
        - "배수 시스템"
        - "안전 구역"
        - "SOLAS 규정"
  
  safety:
    display_name: "선박 안전 규정"
    description: "선박 안전 장비 및 절차 전문가"
    module_path: "agents.safety_agent.agent"
    bedrock_agent_id: "SAFETY_AGENT_ID"  # 실제 ID로 교체 필요
    bedrock_alias_id: "SAFETY_ALIAS_ID"  # 실제 ID로 교체 필요
    knowledge_base_id: "SAFETY_KB_ID"    # 실제 ID로 교체 필요
    lambda_function_name: "safety-lambda"
    enabled: false  # 아직 구현되지 않음
    ui_config:
      icon: "⚠️"
      color: "#FFA500"
      topics:
        - "구명 장비"
        - "비상 절차"
        - "안전 훈련"
        - "개인 보호 장비"
        - "위험 물질 관리"
  
  navigation:
    display_name: "항해 규정"
    description: "항해 규칙 및 해상 교통 전문가"
    module_path: "agents.navigation_agent.agent"
    bedrock_agent_id: "NAV_AGENT_ID"     # 실제 ID로 교체 필요
    bedrock_alias_id: "NAV_ALIAS_ID"     # 실제 ID로 교체 필요
    knowledge_base_id: "NAV_KB_ID"       # 실제 ID로 교체 필요
    lambda_function_name: "navigation-lambda"
    enabled: false  # 아직 구현되지 않음
    ui_config:
      icon: "🧭"
      color: "#4ECDC4"
      topics:
        - "충돌 방지 규칙"
        - "항로 표지"
        - "통신 절차"
        - "기상 대응"
        - "항만 규정"

# 글로벌 설정
global_config:
  aws_region: "us-west-2"
  default_language: "ko"
  session_timeout: 3600  # 1시간
  max_message_length: 4000
  enable_tracing: true